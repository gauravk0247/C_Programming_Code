#include <stdio.h>

int main(){
	float pi = 3.14;
	int radius = 4;
	printf("Area of circle is %f \n", pi*radius*radius);
	int height = 3;
	printf("Volume of this cylinder is %f \n", pi*radius*radius*height);
	return 0;
}
