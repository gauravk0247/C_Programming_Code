#include <stdio.h>
float fib (float n);
int main(){
    float a;
    printf("Enter a number \n");
    scanf("%f", &a);
    printf("fib series is ");
    return 0;
}
float fib(float fib){
    float(n) = a(n-1) + a(n-2);
    return a(n);
}